#!/usr/bin/env bash
set -euo pipefail

: "${MINICLOUD_NODE_AGENT_CONFIG_FILE:?MINICLOUD_NODE_AGENT_CONFIG_FILE is required}"
: "${MINICLOUD_NODE_AGENT_BIN:?MINICLOUD_NODE_AGENT_BIN is required}"

exec "$MINICLOUD_NODE_AGENT_BIN" --config "$MINICLOUD_NODE_AGENT_CONFIG_FILE"
