global:
  resolve_timeout: 5m

route:
  receiver: local-ui
  group_by:
    - alertname
    - severity
    - plane
    - plane_id
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 2h

receivers:
  - name: local-ui
